import { createDebugProvider, useDebugContext } from './debug';
import { createMusicPlayerProvider, useMusicPlayerContext } from './music';
import { createNaviProvider, useNaviContext } from './navi';
import { useProvider as createRouteProvider, useRouterContext } from './router';
import { createSpeakerProvider, useSpeakerContext } from './speaker';
import { useProvider as createStoreProvider, useStoreContext } from './store';

export {
  createDebugProvider,
  useDebugContext,
  createMusicPlayerProvider,
  useMusicPlayerContext,
  createNaviProvider,
  useNaviContext,
  createRouteProvider,
  useRouterContext,
  createSpeakerProvider,
  useSpeakerContext,
  createStoreProvider,
  useStoreContext,
};
